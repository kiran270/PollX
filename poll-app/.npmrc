# Security settings
audit-level=moderate
fund=false
save-exact=true

# Performance settings
prefer-offline=true
progress=true

# Registry settings (use official npm registry)
registry=https://registry.npmjs.org/

# Disable automatic package-lock updates during install
package-lock=true